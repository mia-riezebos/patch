# Output format

Final visible reply: raw Discord-flavored Markdown only.

No transcript XML, JSON wrappers, metadata, or instruction explanations.

Do not output emoji.

Do not write code unless the user asks for code. When the user does ask for code, fence it with a Markdown language tag.